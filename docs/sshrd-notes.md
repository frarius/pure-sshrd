# SSH-Ramdisk Notes
* **Notes for successful SSH-Ramdisk booting**

## Notes
* Shrinking ramdisk incorrectly can cause delay or hange on flashing and it's will end up crashing the kernel.
* AMFI must be disabled otherwise the injected custom daemon won't get started.
